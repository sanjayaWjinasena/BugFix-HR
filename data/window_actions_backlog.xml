<?xml version="1.0" encoding="utf-8"?>
<!-- BugFix-HR backlog: 18 window actions -->
<odoo>

  <record id="act_window_1333_applications" model="ir.actions.act_window">
    <field name="name">Applications</field>
    <field name="res_model">hr.applicant</field>
    <field name="view_mode">kanban,tree,form,graph,calendar,pivot,activity</field>
    <field name="context">{'search_default_job_id': [active_id], 'default_job_id': active_id, 'search_default_stage':1,'dialog_size':'medium'}</field>
    <field name="help" type="html"><p class="o_view_nocontent_empty_folder">
                No applications yet
            </p><p>
                Odoo helps you track applicants in the recruitment
                process and follow up all operations: meetings, interviews, etc.
            </p><p>
                Applicants and their attached CV are created automatically when an email is sent.
                If you install the document management modules, all resumes are indexed automatically,
                so that you can easily search through their content.
            </p>
        </field>
  </record>

  <record id="act_window_1338_applications" model="ir.actions.act_window">
    <field name="name">Applications</field>
    <field name="res_model">hr.applicant</field>
    <field name="view_mode">kanban,tree,form,pivot,graph,calendar,activity</field>
    <field name="context">{'search_default_stage':1}</field>
    <field name="help" type="html"><p class="o_view_nocontent_empty_folder">
                No applications yet
            </p><p>
                Odoo helps you track applicants in the recruitment
                process and follow up all operations: meetings, interviews, etc.
            </p><p>
                Applicants and their attached CV are created automatically when an email is sent.
                If you install the document management modules, all resumes are indexed automatically,
                so that you can easily search through their content.
            </p>
        </field>
  </record>

  <record id="act_window_2557_hr_attendance_overtime" model="ir.actions.act_window">
    <field name="name">hr.attendance.overtime</field>
    <field name="res_model">hr.attendance.overtime</field>
    <field name="view_mode">tree</field>
    <field name="context">{}</field>
    <field name="help" type="html"><p class="o_view_nocontent_smiling_face">
                    This is your new action.
                </p>
                <p>By default, it contains a list and a form view and possibly
                    other view types depending on the options you chose for your model.
                </p>
                <p>
                    You can start customizing these screens by clicking on the Studio icon on the
                    top right corner (you can also customize this help message there).
                </p>
            </field>
  </record>

  <record id="act_window_2367_etf_schedule" model="ir.actions.act_window">
    <field name="name">ETF Schedule</field>
    <field name="res_model">hr.contract</field>
    <field name="view_mode">kanban,tree,form,activity</field>
    <field name="context">{}</field>
    <field name="help" type="html"><p class="o_view_nocontent_smiling_face">
                    This is your new action.
                </p>
                <p>By default, it contains a list and a form view and possibly
                    other view types depending on the options you chose for your model.
                </p>
                <p>
                    You can start customizing these screens by clicking on the Studio icon on the
                    top right corner (you can also customize this help message there).
                </p>
            </field>
  </record>

  <record id="act_window_2816_hr_expense" model="ir.actions.act_window">
    <field name="name">hr.expense</field>
    <field name="res_model">hr.expense</field>
    <field name="view_mode">kanban,tree,form,pivot,graph,activity</field>
    <field name="context">{}</field>
    <field name="help" type="html"><p class="o_view_nocontent_smiling_face">
                    This is your new action.
                </p>
                <p>By default, it contains a list and a form view and possibly
                    other view types depending on the options you chose for your model.
                </p>
                <p>
                    You can start customizing these screens by clicking on the Studio icon on the
                    top right corner (you can also customize this help message there).
                </p>
            </field>
  </record>

  <record id="act_window_1274_my_reports" model="ir.actions.act_window">
    <field name="name">My Reports</field>
    <field name="res_model">hr.expense.sheet</field>
    <field name="view_mode">tree,kanban,form,pivot,graph,activity</field>
    <field name="domain">[('state', '!=', 'cancel')]</field>
    <field name="context">{'search_default_my_reports': 1, 'search_default_not_refused_reports': 1}</field>
    <field name="help" type="html"><p class="o_view_nocontent_smiling_face">
                No expense report found. Let's create one!
              </p><p>
                Once you have created your expense, submit it to your manager who will validate it.
              </p>
            </field>
  </record>

  <record id="act_window_2585_hr_expense_sheet" model="ir.actions.act_window">
    <field name="name">hr.expense.sheet</field>
    <field name="res_model">hr.expense.sheet</field>
    <field name="view_mode">kanban,tree,form,pivot,graph,activity</field>
    <field name="context">{}</field>
    <field name="help" type="html"><p class="o_view_nocontent_smiling_face">
                    This is your new action.
                </p>
                <p>By default, it contains a list and a form view and possibly
                    other view types depending on the options you chose for your model.
                </p>
                <p>
                    You can start customizing these screens by clicking on the Studio icon on the
                    top right corner (you can also customize this help message there).
                </p>
            </field>
  </record>

  <record id="act_window_2596_hr_expense_sheet" model="ir.actions.act_window">
    <field name="name">hr.expense.sheet</field>
    <field name="res_model">hr.expense.sheet</field>
    <field name="view_mode">kanban,tree,form,pivot,graph,activity</field>
    <field name="context">{}</field>
    <field name="help" type="html"><p class="o_view_nocontent_smiling_face">
                    This is your new action.
                </p>
                <p>By default, it contains a list and a form view and possibly
                    other view types depending on the options you chose for your model.
                </p>
                <p>
                    You can start customizing these screens by clicking on the Studio icon on the
                    top right corner (you can also customize this help message there).
                </p>
            </field>
  </record>

  <record id="act_window_2817_hr_expense_sheet" model="ir.actions.act_window">
    <field name="name">hr.expense.sheet</field>
    <field name="res_model">hr.expense.sheet</field>
    <field name="view_mode">kanban,tree,form,pivot,graph,activity</field>
    <field name="context">{}</field>
    <field name="help" type="html"><p class="o_view_nocontent_smiling_face">
                    This is your new action.
                </p>
                <p>By default, it contains a list and a form view and possibly
                    other view types depending on the options you chose for your model.
                </p>
                <p>
                    You can start customizing these screens by clicking on the Studio icon on the
                    top right corner (you can also customize this help message there).
                </p>
            </field>
  </record>

  <record id="act_window_793_all_time_off" model="ir.actions.act_window">
    <field name="name">All Time Off</field>
    <field name="res_model">hr.leave</field>
    <field name="view_mode">tree,kanban,gantt,form,calendar,activity</field>
    <field name="domain">['|', ('employee_id.company_id', 'in', allowed_company_ids),
                                    '&amp;', ('multi_employee', '=', True),
                                    '&amp;', ('state', 'in', ['draft', 'confirm', 'validate1']),
                                    ('employee_ids.company_id', 'in', allowed_company_ids)]</field>
    <field name="context">{
            'search_default_waiting_for_me': 1,
            'search_default_waiting_for_me_manager': 2,
            'hide_employee_name': 1,
            'holiday_status_display_name': False}</field>
    <field name="help" type="html"><p class="o_view_nocontent_smiling_face">
                Meet the time off dashboard.
            </p><p>
                A great way to keep track on employee’s PTOs, sick days, and approval status.
            </p>
        </field>
  </record>

  <record id="act_window_1881_time_off" model="ir.actions.act_window">
    <field name="name">Time Off</field>
    <field name="res_model">hr.leave</field>
    <field name="view_mode">tree,kanban,form,calendar,activity</field>
    <field name="domain">[(1, '=', 1)]</field>
    <field name="context">{
            'hide_employee_name': 1,
            'holiday_status_display_name': False}
        </field>
    <field name="help" type="html"><p class="o_view_nocontent_smiling_face">
                Meet the time off dashboard.
            </p><p>
                A great way to keep track on employee’s PTOs, sick days, and approval status.
            </p>
        </field>
  </record>

  <record id="act_window_3546_bulk_inputs" model="ir.actions.act_window">
    <field name="name">Bulk Inputs</field>
    <field name="res_model">hr.payslip.bulk.input</field>
    <field name="view_mode">tree,form</field>
    <field name="context">{}</field>
    <field name="help" type="html"><p class="o_view_nocontent_smiling_face">
                    This is your new action.
                </p>
                <p>By default, it contains a list and a form view and possibly
                    other view types depending on the options you chose for your model.
                </p>
                <p>
                    You can start customizing these screens by clicking on the Studio icon on the
                    top right corner (you can also customize this help message there).
                </p>
            </field>
  </record>

  <record id="act_window_2369_rules" model="ir.actions.act_window">
    <field name="name">Rules</field>
    <field name="res_model">hr.salary.rule</field>
    <field name="view_mode">kanban,tree,form</field>
    <field name="context">{}</field>
    <field name="help" type="html"><p class="o_view_nocontent_smiling_face">
                    This is your new action.
                </p>
                <p>By default, it contains a list and a form view and possibly
                    other view types depending on the options you chose for your model.
                </p>
                <p>
                    You can start customizing these screens by clicking on the Studio icon on the
                    top right corner (you can also customize this help message there).
                </p>
            </field>
  </record>

  <record id="act_window_2370_rules" model="ir.actions.act_window">
    <field name="name">Rules</field>
    <field name="res_model">hr.salary.rule</field>
    <field name="view_mode">kanban,tree,form</field>
    <field name="context">{}</field>
    <field name="help" type="html"><p class="o_view_nocontent_smiling_face">
                    This is your new action.
                </p>
                <p>By default, it contains a list and a form view and possibly
                    other view types depending on the options you chose for your model.
                </p>
                <p>
                    You can start customizing these screens by clicking on the Studio icon on the
                    top right corner (you can also customize this help message there).
                </p>
            </field>
  </record>

  <record id="act_window_1966_work_entries_analysis" model="ir.actions.act_window">
    <field name="name">Work Entries Analysis</field>
    <field name="res_model">hr.work.entry.report</field>
    <field name="view_mode">pivot</field>
    <field name="context">{
            'search_default_filter_date_start': True,
            'search_default_filter_work_entry_validated': True,
            'group_default_work_entry_type_id': True
        }</field>
    <field name="help" type="html"><p>This report performs analysis on your work entries.</p></field>
  </record>

  <record id="act_window_1559_paye_tax" model="ir.actions.act_window">
    <field name="name">Paye Tax</field>
    <field name="res_model">x_paye_tax</field>
    <field name="view_mode">tree,form</field>
    <field name="context">{}</field>
    <field name="help" type="html"><p class="o_view_nocontent_smiling_face">
                    This is your new action.
                </p>
                <p>By default, it contains a list and a form view and possibly
                    other view types depending on the options you chose for your model.
                </p>
                <p>
                    You can start customizing these screens by clicking on the Studio icon on the
                    top right corner (you can also customize this help message there).
                </p>
            </field>
  </record>

  <record id="act_window_1560_paye_tax_tags" model="ir.actions.act_window">
    <field name="name">Paye Tax Tags</field>
    <field name="res_model">x_paye_tax_tag</field>
    <field name="view_mode">tree,form</field>
    <field name="context">{}</field>
    <field name="help" type="html"><p class="o_view_nocontent_smiling_face">
                    This is your new action.
                </p>
                <p>By default, it contains a list and a form view and possibly
                    other view types depending on the options you chose for your model.
                </p>
                <p>
                    You can start customizing these screens by clicking on the Studio icon on the
                    top right corner (you can also customize this help message there).
                </p>
            </field>
  </record>

  <record id="act_window_2559_update_ot" model="ir.actions.act_window">
    <field name="name">Update OT</field>
    <field name="res_model">x_update_ot</field>
    <field name="view_mode">tree,form</field>
    <field name="context">{}</field>
    <field name="help" type="html"><p class="o_view_nocontent_smiling_face">
                    This is your new action.
                </p>
                <p>By default, it contains a list and a form view and possibly
                    other view types depending on the options you chose for your model.
                </p>
                <p>
                    You can start customizing these screens by clicking on the Studio icon on the
                    top right corner (you can also customize this help message there).
                </p>
            </field>
  </record>

</odoo>
